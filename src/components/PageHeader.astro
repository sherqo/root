---
interface PageHeaderProps {
  subtitle?: string;
}

const { subtitle } = Astro.props as PageHeaderProps;
---

<header class="my-8 text-center">
  <h1 class="mb-4">
    <slot />
  </h1>
  {subtitle && <p class="text-muted lead mb-4">{subtitle}</p>}
</header>
